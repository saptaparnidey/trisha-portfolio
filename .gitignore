node_modules/
.env
dist/
public/uploads/*
!public/uploads/.gitkeep
# Thumbnail/profile images uploaded via the admin panel must be in git so
# Netlify can copy them into dist/ during the build.
!public/uploads/*.png
!public/uploads/*.jpg
!public/uploads/*.jpeg
!public/uploads/*.webp
!public/uploads/*.gif
!public/uploads/*.svg
# Work deliverables (PDFs/videos) uploaded via the admin panel.
!public/uploads/work/
!public/uploads/work/**
*.log
.server.pid
server.log
.netlify
.DS_Store
